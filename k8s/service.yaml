apiVersion: v1
kind: Service
metadata:
  name: casino-frontend
  labels:
    app: casino-frontend
spec:
  # LoadBalancer: http://casino-frontend:8080
  type: LoadBalancer
  selector:
    app: casino-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
